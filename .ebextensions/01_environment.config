option_settings:
  aws:elasticbeanstalk:application:environment:
    PORT: 5000
    NODE_ENV: production
    # Note: Do NOT put sensitive values like API keys, secrets, or credentials here
    # Use the Elastic Beanstalk console to configure sensitive environment variables